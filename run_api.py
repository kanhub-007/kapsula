"""Convenience entry point — run with: python run_api.py

All startup logic lives in doc_search/startup/api.py (composition root).
"""

from doc_search.startup.api import run

if __name__ == "__main__":
    run()
