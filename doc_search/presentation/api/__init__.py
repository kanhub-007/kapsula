"""Presentation layer — FastAPI routes and request/response models.

The FastAPI `app` instance lives in the startup layer (composition root):
    from doc_search.startup.api import app

Run with: python -m doc_search.presentation.api
"""
